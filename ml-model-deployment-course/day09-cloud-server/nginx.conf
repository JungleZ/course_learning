# Day 9 云服务器部署参考配置

# Nginx 配置文件
# 放置路径：/etc/nginx/sites-available/ml-api

server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP

    # 请求大小限制（上传文件时需要）
    client_max_body_size 10M;

    # 反向代理到 Gunicorn
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_read_timeout 120s;
    }

    # 健康检查不记日志
    location /health {
        proxy_pass http://127.0.0.1:5000/health;
        access_log off;
    }
}

# systemd 服务文件
# 放置路径：/etc/systemd/system/ml-api.service
#
# [Unit]
# Description=ML Model API Service
# After=network.target
#
# [Service]
# Type=simple
# User=mluser
# Group=mluser
# WorkingDirectory=/opt/ml-app
# Environment="PATH=/opt/ml-app/venv/bin"
# ExecStart=/opt/ml-app/venv/bin/gunicorn \
#     -w 4 \
#     -b 127.0.0.1:5000 \
#     --timeout 120 \
#     --access-logfile /var/log/ml-api/access.log \
#     --error-logfile /var/log/ml-api/error.log \
#     app:app
# Restart=always
# RestartSec=5
#
# [Install]
# WantedBy=multi-user.target
